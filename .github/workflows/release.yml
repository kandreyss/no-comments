name: Build and Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: no-comments-linux-x64
            extension: ""
          - os: macos-latest
            artifact_name: no-comments-darwin-arm64
            extension: ""
          - os: macos-13
            artifact_name: no-comments-darwin-x64
            extension: ""
          - os: windows-latest
            artifact_name: no-comments-windows-x64
            extension: ".exe"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile (Linux/macOS)
        if: matrix.os != 'windows-latest'
        run: |
          gcc -O3 no-comments.c -o ${{ matrix.artifact_name }}${{ matrix.extension }}
          if [[ "$OSTYPE" == "linux-gnu"* ]]; then strip ${{ matrix.artifact_name }}; fi

      - name: Compile (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          gcc -O3 no-comments.c -o ${{ matrix.artifact_name }}${{ matrix.extension }}

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact_name }}${{ matrix.extension }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
