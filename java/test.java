package com.test;

/**
 * Класс Application.
 * Javadoc должен быть СОХРАНЕН.
 */
public class Application {

    // Временная переменная (УДАЛИТЬ)
    private int counter;

    /**
     * Конструктор.
     * (ОСТАВИТЬ)
     */
    public Application() {
        // Инициализация (УДАЛИТЬ)
        this.counter = 0;
    }

    /*
     * Блок комментариев разработчика.
     * Не является документацией.
     * (УДАЛИТЬ)
     */

    /// Нестандартный для Java стиль, но утилита должна его ОСТАВИТЬ (///)
    public void run() {
        System.out.println("Hello"); // Print (УДАЛИТЬ)
    }
}
