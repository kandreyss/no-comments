/**
 * @function calculateArea
 * @param {number} radius
 * @returns {number}
 * ЭТОТ JSDoc ДОЛЖЕН ОСТАТЬСЯ.
 */
function calculateArea(radius) {

    return Math.PI * radius * radius;
}

/// @ts-check (Директива TypeScript, должна ОСТАТЬСЯ)
const x = 10;

let y = 20;

/** Константа конфигурации (ОСТАВИТЬ) */
const CONFIG = {
    debug: true
};
