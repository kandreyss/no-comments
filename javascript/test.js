/**
 * @function calculateArea
 * @param {number} radius
 * @returns {number}
 * ЭТОТ JSDoc ДОЛЖЕН ОСТАТЬСЯ.
 */
function calculateArea(radius) {
    // Формула площади круга (УДАЛИТЬ)
    return Math.PI * radius * radius;
}

/* 
   TODO: Переписать эту функцию
   (УДАЛИТЬ)
*/

/// @ts-check (Директива TypeScript, должна ОСТАТЬСЯ)
const x = 10;

// Просто переменная (УДАЛИТЬ)
let y = 20;

/** Константа конфигурации (ОСТАВИТЬ) */
const CONFIG = {
    debug: true // Режим отладки (УДАЛИТЬ)
};
