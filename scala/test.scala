/**
 * Объект Singleton.
 * ScalaDoc (ОСТАВИТЬ).
 */
object Main extends App {
    // Точка входа (УДАЛИТЬ)
    println("Hello Scala")

    /* 
      Многострочный комментарий
      (УДАЛИТЬ)
    */

    /** Метод вычисления (ОСТАВИТЬ) */
    def compute(x: Int): Int = {
        x * 2 // Умножение (УДАЛИТЬ)
    }

    /// Быстрый комментарий (ОСТАВИТЬ)
    val version = "1.0"
}
